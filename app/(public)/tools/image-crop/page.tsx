export const metadata = {
  title: "Free Image Crop Tool Online | UdaanPath",
  description:
    "Crop image online for govt forms, profile photo, documents, thumbnails. Free, fast & works on mobile.",
};

import ImageCropTool from "@/ui/tools/ImageCropTool";

export default function ImageCropPage() {
  return <ImageCropTool />;
}
